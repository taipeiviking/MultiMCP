node_modules/
dist/
out/
build/
# bundled uv binaries are fetched at build time (scripts/fetch-uv.js), not committed
vendor/
# but DO track the NSIS installer customization (source, not build output)
!build/
!build/installer.nsh
build/*
!build/installer.nsh
*.log
dist-build.log
.DS_Store
.claude/settings.local.json
.env
.env.*
# never commit user data or backups
settings.json
accounts.json
claude_desktop_config.json.bak-*

# redundant scaffold archive (superseded by extracted tree)
google-workspace-manager.zip

# self-signed code-signing material (never commit private key)
build/selfsign/

# python (multimcp-signal dev env)
python/**/.venv/
python/**/__pycache__/
python/**/*.egg-info/
uv.lock

# session working notes (local only)
_SESSION-CONTEXT-*.md

# electron-builder installer output (separate from vite dist/)
release/
